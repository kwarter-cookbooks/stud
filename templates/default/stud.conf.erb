#/bin/bash
# The apt-get version conf differs much from the version found on GITHUB
# Please don't ask me how I figured this out. 
# Generated by Chef for <%= node[:fqdn] %>

OPTIONS="-q --ssl -b <%= node[:stud][:v4local] %>,<%= node[:stud][:rport] %> -f *,<%= node[:stud][:lport] %> <%= @ssl_cert %>"
